type Query {
    bookById(id: ID): [Book]
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
    prevBook: Book
}

type Author {
    id: ID
    firstName: String
    lastName: String
    bookCount: Int
}